"use client";

import Link from "next/link";
import { useMemo, useState } from "react";
import { bs, gbmPath, martingaleDiagnostic, monteCarlo, type Inputs } from "../lib/quant";

const initial: Inputs = { spot: 100, strike: 100, maturity: 30 / 365, vol: .22, rate: .05, dividend: 0, paths: 10000, seed: 42 };
const money = (n: number) => `$${n.toFixed(2)}`;

const nav = [
  ["Trading Arena", "/arena"], ["Pricing Laboratory", "/"], ["Strategy Laboratory", "/strategy"],
  ["Stress Laboratory", "/stress"], ["Untouched Validation", "/validation"], ["Multiple-Testing Audit", "/inference"],
  ["Decision Autopsy", "/autopsy"], ["Strategy Tribunal", "/tribunal"], ["Methodology", "/methodology"]
] as const;

export default function Home() {
  const [inputs, setInputs] = useState(initial);
  const [measure, setMeasure] = useState<"P" | "Q">("Q");
  const price = useMemo(() => bs(inputs), [inputs]);
  const mc = useMemo(() => monteCarlo(inputs), [inputs]);
  const path = useMemo(() => gbmPath(inputs, measure), [inputs, measure]);
  const diagnostic = useMemo(() => martingaleDiagnostic(inputs), [inputs]);
  const update = (key: keyof Inputs) => (value: number) => setInputs(current => ({ ...current, [key]: value }));
  return <main><aside><div className="brand"><div className="mark">∑</div><div><strong>PROJECT TBD</strong><small>QUANT LAB / 0.1</small></div></div><nav>{nav.map(([label, href], index) => <Link key={label} href={href} className={href === "/" ? "nav-link nav-current" : "nav-link"}><span className="nav-index">{String(index + 1).padStart(2, "0")}</span>{label}{href === "/arena" && <b className="live-dot" />}</Link>)}</nav><div className="sidebar-bottom"><div className="status"><span className="dot"/>ENGINE ONLINE</div><p>Educational simulator<br/>Synthetic data only</p><div className="seed">SEED <b>{inputs.seed}</b></div></div></aside><section className="workspace"><header><div><p className="eyebrow">RESEARCH WORKBENCH / SESSION 014</p><h1>Pricing Laboratory</h1></div><div className="header-actions"><span className="chip"><span className="dot"/>SYNTHETIC</span><div className="avatar">QT</div></div></header><div className="toolbar"><div className="toolbar-title"><span className="signal"/>BENCHMARK CONFIGURATION</div><button className="run" onClick={() => update("seed")(inputs.seed + 1)}>↻ Re-run seeded experiment</button></div><div className="lab-grid"><section className="panel config"><div className="panel-head"><span>01 / PARAMETERS</span><em>GBM + EUROPEAN CALL</em></div><div className="fields"><Field label="Spot S₀" value={inputs.spot} onChange={update("spot")} suffix="$"/><Field label="Strike K" value={inputs.strike} onChange={update("strike")} suffix="$"/><Field label="Maturity T" value={inputs.maturity * 365} onChange={n => update("maturity")(n / 365)} suffix="DAYS"/><Field label="Volatility σ" value={inputs.vol * 100} onChange={n => update("vol")(n / 100)} step="0.01" suffix="%"/><Field label="Rate r" value={inputs.rate * 100} onChange={n => update("rate")(n / 100)} step="0.01" suffix="%"/><Field label="Paths M" value={inputs.paths} onChange={update("paths")} step="100"/><Field label="Seed" value={inputs.seed} onChange={update("seed")} step="1"/></div><div className="measure"><span>Measure</span><button className={measure === "P" ? "selected" : ""} onClick={() => setMeasure("P")}>P · physical</button><button className={measure === "Q" ? "selected" : ""} onClick={() => setMeasure("Q")}>Q · risk-neutral</button></div><p className="helper">P uses real-world drift. Q replaces it with <b>r − q</b> for no-arbitrage pricing and martingale diagnostics.</p></section><section className="panel chart-panel"><div className="panel-head"><span>02 / PATH GENERATION</span><em>{measure} MEASURE · SEEDED</em></div><div className="chart-title"><div><strong>Simulated spot path</strong><small>Exact GBM discretization · 30 steps</small></div></div><Spark data={path} label={`${measure} measure simulated spot path`} /><div className="chart-foot"><span>S₀ {money(inputs.spot)}</span><span>T {inputs.maturity.toFixed(3)} yr</span><span>terminal {money(path.at(-1) ?? 0)}</span></div></section></div><div className="metrics"><Metric label="BLACK–SCHOLES CALL" value={money(price.call)} sub="analytic benchmark"/><Metric label="MONTE CARLO ESTIMATE" value={money(mc.price)} sub={`95% CI ${money(mc.low)} — ${money(mc.high)}`} tone="teal"/><Metric label="SAMPLING ERROR" value={money(mc.se)} sub={`${mc.paths.toLocaleString()} paths`} tone="gold"/><Metric label="PARITY RESIDUAL" value={money(price.call - price.put - inputs.spot * Math.exp(-inputs.dividend * inputs.maturity) + inputs.strike * Math.exp(-inputs.rate * inputs.maturity))} sub="C − P − S e⁻qT + K e⁻rT" tone="teal"/></div><div className="lower-grid"><section className="panel"><div className="panel-head"><span>03 / ANALYTIC DIAGNOSTICS</span><em>BLACK–SCHOLES</em></div><div className="greeks">{[["DELTA", price.delta], ["GAMMA", price.gamma], ["VEGA", price.vega], ["THETA", price.theta], ["RHO", price.rho]].map(([label, value]) => <div key={label as string}><span>{label}</span><b>{Number(value).toFixed(4)}</b></div>)}</div><div className="equation">C = S e<sup>−qT</sup> N(d₁) − K e<sup>−rT</sup> N(d₂)</div><p className="helper">Analytic value is {price.call >= mc.low && price.call <= mc.high ? <span className="ok">inside</span> : <span className="warn">outside</span>} the reported 95% interval.</p></section><section className="panel"><div className="panel-head"><span>04 / MARTINGALE CHECK</span><em>DISCOUNTED UNDERLYING</em></div><div className="diagnostic"><div className="diag-ring"><span>{Math.abs(diagnostic.error / diagnostic.expected * 100).toFixed(1)}%</span><small>error</small></div><div><strong>Q diagnostic</strong><p>E[S₀e<sup>−qT</sup>] {money(diagnostic.expected)}</p><p>Observed discounted {money(diagnostic.observed)}</p><span className="ok">● reproducible · seed {inputs.seed}</span></div></div><div className="formula">V<sub>t</sub> = e<sup>−r(T−t)</sup> E<sup>Q</sup>[ Φ(S<sub>T</sub>) | F<sub>t</sub> ]</div></section></div><div className="footnote"><span>ENGINE · JavaScript reference implementation</span><span>STATUS · <b className="ok">VALIDATED</b></span><span><Link href="/methodology" className="nav-link-inline">READ METHODOLOGY →</Link></span></div></section></main>;
}

function Field({ label, value, onChange, step = "any", suffix }: { label: string; value: number; onChange: (n: number) => void; step?: string; suffix?: string }) { return <label className="field"><span>{label}</span><div className="input-wrap"><input type="number" value={value} step={step} onChange={event => onChange(Number(event.target.value))}/>{suffix && <i>{suffix}</i>}</div></label>; }
function Spark({ data, color = "#65d6bd", label }: { data: number[]; color?: string; label: string }) { const min = Math.min(...data), max = Math.max(...data), range = max - min || 1; const points = data.map((value, index) => `${(index / (data.length - 1)) * 100},${96 - ((value - min) / range) * 82}`).join(" "); return <div className="spark-wrap"><svg viewBox="0 0 100 100" preserveAspectRatio="none" role="img" aria-label={label}><polyline points={points} fill="none" stroke={color} strokeWidth="1.8" vectorEffect="non-scaling-stroke"/></svg><div className="spark-label"><span>{money(min)}</span><span>{money(max)}</span></div></div>; }
function Metric({ label, value, sub, tone = "" }: { label: string; value: string; sub: string; tone?: string }) { return <div className={`metric ${tone}`}><span>{label}</span><strong>{value}</strong><small>{sub}</small></div>; }
